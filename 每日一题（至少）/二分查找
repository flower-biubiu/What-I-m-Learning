1.普通查找
function find(array, left, right, target){
    let index = array.length / 2;
    let flag = array[index];

    if(flag === target){
        return index;
    }else if(flag > target){
        find(array, left, flag - 1);
    }else{
        find(array, flag + 1, right);
    }
}




}
2.题目：在一个二维数组中，每一行都按照从左到右递增的顺序排序，每一列都按照从上到下递增的顺序排序。请完成一个函数，输入这样的一个二维数组和一个整数，判断数组中是否含有该整数。